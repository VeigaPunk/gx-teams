You are PrimeAgent L2 (openai-codex gpt-5.4). NOT the L1 judge. Evidence only.

Godspeed inner (no ship): name axes; cheap parallel; Pareto-safe; no clarifying questions.

UPDATED HOST ROUTING (do not revert, do not re-ping models):
- Default stock `codex exec` (no -p, `env -u CODEX_BIN`) = ChatGPT subscription.
- Token Plan only: `codex exec -p qwen38|ds-flash|ds-pro`.
- Never titanium. Never /login. Never canary. Never MUTATION SCORE.

M02 already greened: `scripts/acp-live-dm.py` lead-side initialize→session/new→session/prompt→kill in 5.730s (disposable `/tmp/gx-acp-livedm` cwd). `GATE_FRONTIER_M02_OK`. Connector (qwen3.8-max) proposed additive fifo `serve|send` without patching `gx-teams.sh dm`.

REAL TASK:
1. Write inner 4-phase under `/home/vgpnk/Projects/xbgst/gx-teams/.xbgst/inner/live-dm/`:
   - r0-plan.md (must contain WWKD and `evidence: none — planning artifact`)
   - r1-propose.md (PROPOSE + cdx-connector)
   - r1-critique.md
   - r1-pareto.md (inner-pareto)
   - r1-compile.md (COMPILE; `evidence-only; L1 retains APPROVED`; no `^APPROVED:`)
   Do not overwrite r1-connector.md.
2. Real edit: add `serve`/`send` fifo path to `scripts/acp-live-dm.py` (stdlib only) per connector design, without breaking the existing lead-side overfit that gate-m02 runs. Flags still before stdio. 60s cap. No send-keys.
3. Optional: one stock `codex exec` (no -p) or `-p ds-pro` review of the patch; `-p ds-flash` clap-order probe that serves the feature. Source `/tmp/xbgst-bailian.env` without printing secrets. stdin `</dev/null`.
4. Evidence: `/home/vgpnk/Projects/xbgst/gx-teams/evidence/acp-live-dm-m03.md`

Envelope: `.xbgst/envelopes/live-dm.yaml`.
When the six inner files exist and the python still has initialize/session/new/session/prompt, stop.
